# Most serious notes — source-verified audit, September 10, 2026

Source: `/Users/varsityhub/Desktop/Most serious notes.pdf`, all six pages, text and screenshots inspected.

The notes identify real defects, but also contain product decisions and features that do not exist yet. They are useful evidence, not a complete specification or authorization to delete production records. This is a targeted audit of their claims, not a claim that every rule in the repository has been exhaustively audited.

## Findings and action

| Note                                                       | Classification and current result                                                                                                                                                                                                                                                                                                                                                                         | Source / evidence                                                                                                                    |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Only founder-controlled accounts should receive exceptions | **Verified security bug fixed locally:** `[DEMO_MATCHUP]` in descriptive content bypassed geofencing for any uploader. Removed from posts, game stories, and event stories. Verified admin identities and explicit event grants remain. **Policy decision pending:** whether App Store review admin access should remain.                                                                                 | `server/src/routes/posts.ts`, `games.ts`, `events.ts`; `server/src/middleware/requireAdmin.ts`; `server/src/lib/adminEmails.ts`      |
| Pin colors disagree with the legend                        | **Fixed locally:** colors came from explicit marker color, home/away team colors, sport color, then generic type. Legend now explains varying team/sport colors; cluster behavior remains. Native/web reuse `utils/mapMarkerColor.ts`.                                                                                                                                                                    | `components/EventMap.tsx`, `components/EventMap.web.tsx`                                                                             |
| Gold pins for pages with posts                             | **Implemented locally:** discovery supplies `has_posts` from filtered post counts; gold overrides team colors. Counts exclude deleted posts, blocked authors, inaccessible private profiles and private teams using the existing privacy helper. Text-only posts count too.                                                                                                                               | `server/src/routes/eventDiscovery.ts`, `server/src/lib/eventDiscovery.ts`, `utils/mapDiscovery.ts`, `apiclient/schemas/eventCard.ts` |
| Black borders in post video                                | **Fixed locally:** inline post-detail player explicitly uses `contentFit="cover"`, filling its frame. Fullscreen keeps its existing fit behavior. This crops the edges of mismatched aspect ratios; bars baked into a source video are not removed.                                                                                                                                                       | `app/post-detail.tsx`, `components/VideoPlayer.tsx`                                                                                  |
| Past-date maps should contain only pages with posts        | **Fixed locally:** explicit past-date queries no longer exempt empty pages. Both game and standalone-event paths require viewer-visible posts. Selected current-day schedules retain already-started fixtures. **No records deleted:** retention is a separate decision because related records can still have value.                                                                                     | `server/src/lib/eventDiscovery.ts`; HTTP privacy test confirms four test events remain in the database while only one is visible     |
| Search should prioritize nearby dates                      | **Fixed locally:** game/event name searches fetch bounded nearest candidates before and after the anchor, merge by absolute time distance, then limit. Upcoming wins equal-distance ties. Explicit date queries retain their date window. Map text-search orders its loaded matches by time distance.                                                                                                     | `server/src/lib/searchTemporalRanking.ts`, `server/src/routes/search.ts`, `components/EventMap.tsx`                                  |
| Duplicate navigation/loading screens                       | **One concrete redirect removed:** shared event href now opens `/game/[id]?eventId=...` directly, including notification/edit-event links. Legacy inbound deep links remain supported. Existing map double-tap deduplication tests pass. **Not a blanket device closure:** the screenshot alone cannot prove every observed transition shares this cause.                                                 | `utils/eventRoutes.ts`, `__tests__/event-route-helper-contract.test.ts`, `components/__tests__/EventMap.test.tsx`                    |
| Pin the current venue's live game to the top               | **Already implemented:** `shouldPinToFeed` requires a live event and known viewer/venue coordinates within 3 km; `app/feed.tsx` renders pinned events before other feed items. Existing tests pass. Location permission and a returned event are prerequisites. No physical-device verification in this task.                                                                                             | `utils/liveWindow.ts`, `app/feed.tsx`, `utils/__tests__/liveWindow.test.ts`                                                          |
| Minor league map is empty                                  | **Coverage gap:** `PRO_SCHEDULE_LEAGUES` configures major/pro and NCAA feeds, not minor leagues. Catalog/filter support does not create fixtures. **Fixed misleading copy:** empty results no longer claim coordinates must be missing. **Open limitation:** sport/level filtering after a bounded discovery fetch can also underfill results; absence of pins is not proof of absence from the database. | `server/src/lib/proSchedule/types.ts`, `server/src/lib/eventDiscovery.ts`, `app/game-map.tsx`, `components/EventMap.tsx`             |
| Change wrestling to “combat p”                             | **Policy decision pending:** clarify a “Combat sports” category versus renaming the wrestling sport. Canonical wrestling data was preserved.                                                                                                                                                                                                                                                              | `constants/sports.ts`, `shared/sports-taxonomy.json`                                                                                 |

## Security proof and risk

The removed description bypass was exploitable by ordinary accounts uploading to tagged pages, across three sibling write paths. A database-backed HTTP regression created an approved game/event with `[DEMO_MATCHUP]` and sent uploads without location. Before the fix, `/posts` and game stories returned **201**. After the fix, posts and both story routes return **403**. The blast radius was event posting integrity; the route fix is reversible, while previously created content requires separate review.

The privacy regression creates public, blocked, and private authors plus four past events. An authenticated discovery request exposes only the event with a visible public post, sets its gold-pin flag, and retains all four database records. No separate, weaker privacy mechanism was introduced.

## Relevant rules, frontend and backend

| Rule                    | Frontend behavior                                                          | Authoritative backend                                                                                                                                                                                                                                                                                            |
| ----------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Platform administration | UI may hide/show actions, but cannot grant access                          | `requireAdmin` / `getIsAdmin` re-read the user and require verified email plus the platform access allowlist. App review identity is currently included automatically. Usernames such as `@superfan` are not sufficient proof of ownership.                                                                      |
| First event post        | Location prompt and safe error messages guide the user                     | `verifyEventPostingPermission` checks event window, venue coordinates and device-origin location; a successful visit earns the existing seven-day post unlock. Verified admins and explicit event grants have existing exceptions.                                                                               |
| Stories                 | Composer collects media/location and enforces a duration UX limit          | `verifyStoryPostingPermission` normally requires venue presence on each story within the live window. An ordinary posting unlock alone does not allow stories from elsewhere. A designated-poster row plus active unlock is an explicit exception. Media ownership/readiness and duration remain server checked. |
| Map content indicators  | Render the server's `has_posts`; never infer it from hidden data           | Existing `getPostVisibilityFilters` excludes blocked/private authors and private-team content before counts and past-page inclusion.                                                                                                                                                                             |
| Event approval/privacy  | Show returned cards and route to the canonical page                        | Discovery retains approval/opponent-approval checks and private-team visibility. Gold-pin status grants no permission.                                                                                                                                                                                           |
| Team staff              | Staff controls are scoped to the appropriate actions                       | `canAdministerTeam` and `canManageTeam` are separate tiers; removing the description bypass does not grant staff platform-admin powers.                                                                                                                                                                          |
| Live venue pinning      | `shouldPinToFeed` ranks an already-returned live event within 3 km         | Feed visibility and upload permissions remain separate server checks; pinning is not authorization.                                                                                                                                                                                                              |
| Search                  | Display grouped nearest-date results; map search only searches loaded pins | Both time-direction queries retain the original approval and privacy predicates and bounded result limits.                                                                                                                                                                                                       |
| Errors                  | Existing safe error mapper and telemetry boundary remain                   | Existing error envelope and disclosure scanner remain; no raw error output introduced by these changes.                                                                                                                                                                                                          |

For the broader architecture and security rules, use the authoritative `AGENTS.md` and `docs/ARCHITECTURE.md`. A literal inventory of every condition in the codebase is not a meaningful assurance of correctness; each important invariant needs its own executable proof.

## Verification and release scope

Run with Node 20. Client TypeScript and server TypeScript passed. Navigation audit reported zero REVIEW items; conflict, secret-literal and error-disclosure scans passed. The error-envelope wrapper reported no server changes in its Git comparison, so that message is not evidence of a full server diff audit; targeted HTTP regressions provide the relevant proof.

Client targeted suites: EventMap, event routing, notification presentation, live-window/pinning, and map discovery. Server targeted suites: description-permission regression, unified and temporal search, discovery contract/filter/following/content/privacy, and media recovery. **Results: 88 client tests and 42 server tests passed (130 total).**

These changes are in the existing working tree, alongside earlier user work. No commit, push, Railway deployment, EAS submission, native build, or OTA publication was performed for this PDF task. Server changes require a tested server release; client changes require a compatible client update. No production data or configuration was changed. No native-device visual QA was performed.

## Production follow-up

The user subsequently authorized production deployment. Server and both client runtimes are now released; see [production release evidence](release/SERIOUS_NOTES_PRODUCTION_2026-09-10.md). The local-only statement above records the original audit stage.
